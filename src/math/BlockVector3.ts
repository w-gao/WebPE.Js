export class BlockVector3 {

    public x: number;       // int
    public y: number;       // int
    public z: number;       // int

    constructor(x: number, y: number, z: number) {

        this.x = x;
        this.y = y;
        this.z = z;
    }

}
